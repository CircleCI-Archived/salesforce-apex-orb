# Team down scratch org.
description: >
  Removes a scratch organization.
parameters:
  scratch-alias:
    description: "The alias of the scratch Org."
    type: string

steps:
  - sfdx/scratch-delete:
      scratch-alias: <<parameters.scratch-alias>>
